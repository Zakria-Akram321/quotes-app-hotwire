<div class="flex justify-center items-center flex-col min-h-[90vh] gap-3">  
  <h1 class="text-4xl font-semibold">Quote editor</h1>
  <p>A blazing fast quote editor built with Hotwire</p>

  <% if user_signed_in? %>
    <%= link_to "View quotes", quotes_path, class: "bg-[#df5034] px-7 py-3 text-white font-semibold capitalize inline-block rounded-lg" %>
  <% else %>
    <%= link_to "Sign in", new_user_session_path, class: "bg-[#df5034] px-7 py-3 text-white font-semibold capitalize inline-block rounded-lg" %>
  <% end %>
</div>